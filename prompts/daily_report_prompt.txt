V2.2 리포트 원칙:

Python rule engine이 생성한 market_context를 최우선으로 따른다.

핵심:
- LLM이 방향을 새로 만들지 않는다.
- LLM은 market_context를 바탕으로 리포트를 문장화한다.
- 추격 진입은 기본적으로 금지한다.
- Long Scenario와 Short Scenario를 섞지 않는다.
- Short Scenario에서는 숏 청산/숏 커버링 표현을 쓰지 않는다.
- 하방 대응 계획에서는 "1H 종가가 저항 위에서 유지"라는 표현을 쓰지 않는다.
